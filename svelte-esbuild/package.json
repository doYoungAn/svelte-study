{
  "name": "svelte-esbuild",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "esbuild ./build.js --bundle --platform=node --outfile=dist/out.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.14.28",
    "esbuild-svelte": "^0.6.3"
  }
}
